# 周报数据不进版本库
#
# data/ 下存的是导入的周报原文和条目，可能含公司内部信息（项目名、人员、
# 进度、阻塞原因）。设计方案里「周报内容能否外发」这条还没有结论，
# 在拿到结论前一律只留本地。
#
# 目录不需要手工建，server.js 启动时会自动创建。
data/

# 本地配置：要填 OA 账号密码，绝不能进版本库。
# 模板是 config.example.json，首次启动 server.js 会自动复制一份。
config.json

# 编辑器和系统临时文件
# ~$ 开头的是 Office 打开文档时留下的锁文件
~$*
.DS_Store
Thumbs.db
desktop.ini
*.tmp
*.swp
.vscode/
.idea/

# 本项目零依赖，理论上不会有；留着防止误装
node_modules/
package-lock.json
